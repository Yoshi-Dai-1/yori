# コーディング規約

<!-- このファイルを各プロジェクトの .claude/coding-conventions.md に置く -->
<!-- project-context.md と合わせて使う -->

## 基本方針

このプロジェクトのコードは以下の優先順位で判断する：

1. **正確性**：バグがないこと
2. **可読性**：名前を見ただけで意図が分かること
3. **保守性**：変更しやすい構造であること
4. **効率性**：パフォーマンス（過度な最適化は避ける）

---

## TypeScript 規約

```typescript
// ✅ 良い
const getUserById = async (id: string): Promise<User | null> => { ... };

// ❌ 悪い：any は使わない
const getUser = async (id: any): Promise<any> => { ... };

// ✅ 良い：型引数で明示
const response = await fetch<ApiResponse<User>>('/api/users/1');

// ✅ 良い：unknown で受けてから型ガード
const data: unknown = JSON.parse(raw);
if (isUser(data)) { ... }

// ✅ 良い：const assertion で定数オブジェクトを不変にする
const HTTP_STATUS = {
  OK: 200,
  NOT_FOUND: 404,
} as const;

// ✅ 良い：型エイリアスで意味を与える
type UserId = string;  // string と互換だが意図が明確
type StockCode = string;
```

---

## 関数の書き方

```typescript
// ✅ 良い：単一責任・動詞始まり・型明示
const formatCurrency = (amount: number, currency: 'JPY' | 'USD' = 'JPY'): string => {
  return new Intl.NumberFormat('ja-JP', { style: 'currency', currency }).format(amount);
};

// ✅ 良い：早期リターンで深いネストを避ける
const validateStock = (stock: Stock): ValidationResult => {
  if (!stock.code) return { valid: false, reason: 'コードが未設定' };
  if (!stock.name) return { valid: false, reason: '銘柄名が未設定' };
  return { valid: true };
};

// ❌ 悪い：深いネスト
const validateStock = (stock: Stock): ValidationResult => {
  if (stock.code) {
    if (stock.name) {
      return { valid: true };
    } else {
      return { valid: false, reason: '銘柄名が未設定' };
    }
  } else {
    return { valid: false, reason: 'コードが未設定' };
  }
};
```

---

## コメントの書き方

```typescript
// ❌ 悪い：何をしているか（コードを読めば分かる）
// userIdからユーザーを取得する
const user = await getUserById(userId);

// ✅ 良い：なぜそうしているか（コードから読み取れない意図）
// EDINETのAPIは同一IPから1秒に10回以上リクエストすると429を返すため待機する
await sleep(200);

// ✅ 良い：TODO は担当者と期限を明記
// TODO(keisuke): v2 API移行後に削除する (2025-Q3)
const legacyEndpoint = '/api/v1/stocks';
```

---

## エラーハンドリング

```typescript
// ❌ 悪い：握りつぶし
try {
  await fetchStocks();
} catch (e) {
  // 何もしない
}

// ❌ 悪い：console.log だけ
try {
  await fetchStocks();
} catch (e) {
  console.log(e);
}

// ✅ 良い：ログ出力 + 上位に伝播（またはユーザーへのフィードバック）
try {
  await fetchStocks();
} catch (error) {
  logger.error('株式データの取得に失敗', { error, context: 'fetchStocks' });
  throw new AppError('FETCH_FAILED', '株式データの取得に失敗しました', 500);
}
```

---

## Python 規約（データパイプライン・バックエンドの場合）

```python
# ✅ 良い：型ヒント必須
def calculate_roe(net_income: float, equity: float) -> float:
    if equity == 0:
        raise ValueError("自己資本がゼロです")
    return net_income / equity

# ✅ 良い：dataclass または Pydantic で型安全なデータ構造
from dataclasses import dataclass

@dataclass
class StockSummary:
    code: str
    name: str
    market_cap: int
    per: float | None

# ✅ 良い：f-string でフォーマット
logger.info(f"銘柄 {stock.code} の処理完了: {elapsed:.2f}秒")
```

---

## ファイル構造の規約

```
1ファイル = 1つの責務
ファイルの先頭コメントに「このファイルがやること」を1文で書く

# 例
# stock.service.ts
# 銘柄データのビジネスロジックを提供する（APIコール・DB操作は行わない）
```

---

## AIへのお願い

コードを書く・修正するとき：

1. **既存のスタイルに合わせる**：新しいパターンを勝手に導入しない
2. **影響範囲を先に伝える**：「このファイルのこの関数を変更します」と宣言してから実装する
3. **大きな変更は段階的に**：一度に全部変えず、ステップを分けて提案する
4. **分からないことは質問する**：推測で実装せず、不明点があれば聞く

---

## テスト方針（TDD）

→ 詳細は `.claude/standards/principles/tdd-with-ai.md` を参照。
<!-- 参照先は .claude/standards/principles/ に配置済み（setup-harness.sh が自動コピー） -->

- ビジネスロジック（services・hooks・utils）はTDDで実装する
- テストケースは人間が日本語で定義し、AIがコードに変換する
- テストコードは実装コードより先に書く
- AIが実装を書く際、テストコードは変更しない
- テストが通らない実装は提出しない（エラーとともに修正を継続する）
- 確認順序：型チェック → lint → ユニットテスト → @code-reviewer → 人間レビュー
  （詳細は `.claude/standards/principles/tdd-with-ai.md` を参照）
