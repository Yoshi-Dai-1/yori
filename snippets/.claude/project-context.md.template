# プロジェクトコンテキスト（AI向け）

<!-- このファイルを各プロジェクトの .claude/project-context.md に置く -->
<!-- AIへの指示冒頭に「.claude/project-context.md に従って」と添えるだけで機能する -->
<!-- 設計の詳細（層のルール・非機能要件・禁止パターン）は ARCHITECTURE.md に一元管理する -->
<!-- [] の中を実際の内容に書き換える -->

## プロジェクト概要

**名前**：[プロジェクト名]  
**目的**：[何を作っているか・何のためか 1〜2文]

---

## 設計の詳細

ARCHITECTURE.md を参照。このファイルに以下の情報がある：

- 技術スタック
- アーキテクチャパターン・層のルール・依存の方向
- 命名規則（プロジェクト固有の追加ルール含む）
- 禁止パターン
- 非機能要件（パフォーマンス・セキュリティ・コスト等）
- 決定事項のログ

---

## 現在のタスク

<!-- 毎セッションで更新する。ここだけはARCHITECTURE.mdに書かない動的な情報 -->

**取り組んでいる機能**：[例: スクリーナーのフィルター条件保存]  
**完了している部分**：[例: UI実装済み・APIエンドポイント未実装]  
**次にやること**：[例: POST /api/screener/presets の実装]

---

## AIへの指示スタイル

- 修正前に影響範囲を伝える
- 既存のコードスタイル・命名規則に合わせる
- 変更が大きい場合は段階的に提案する
- 判断に迷ったときは実装前に質問する

## 設定ファイルの自動展開レベル

<!-- AGENTS.md のセットアップ対話（Step 3）でAIが質問し、回答に応じて以下の1行に書き換える -->
<!-- 書き換え前はAIが「記載なし（初回）」と判定し、必ず人間に確認してから展開を実行する -->
未設定
