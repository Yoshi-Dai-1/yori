# コードレビュー常駐ルール（Code Review Rules）

このルールは @code-reviewer 呼び出し時に参照される。
AGENTS.md のコンテキスト消失後も、このルールが常にレビュー判断を補完する。

詳細なレビュー基準は以下を参照（必要時に読む）：
- `.opencode/standards/principles/code-review.md`

---

## 自律トリガー（人間の指示を待たずに実行する）

### @code-reviewer を呼び出す前

1. `.opencode/standards/principles/code-review.md` を読み、レビュー基準を確認する
2. 以下の観点でレビューを実行する：
   - セキュリティ問題がないか
   - パフォーマンス上の問題がないか
   - 可読性・保守性が保たれているか
   - テストカバレッジが十分か
   - 命名規則に一貫性があるか（rules/naming-conventions.md を参照）
   - アーキテクチャの依存方向に違反していないか
3. 問題があれば修正を提案し、問題がなければ承認する

### 実装完了後（型チェック → lint → テスト完了後）

1. 変更ファイルをレビュー基準で確認する
2. 問題を検出したら修正を提案する
3. 問題がなければ人間レビューへ進む

---

## 常駐禁止事項

- セキュリティ問題を「あとで直す」と先送りしない
- テストがないコードを本番ブランチにマージしない
- レビューで検出した問題を修正せずに通過させない
